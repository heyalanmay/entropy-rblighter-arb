# 服务器连接与排查手册（小白版）

> 适用场景：用 vis（VSCode Remote-SSH / 腾讯云 Cloud Studio 等）连服务器跑套利 bot，  
> 结果连接断了，或者 bot 半天没动静，不知道怎么进去看、怎么判断死没死、怎么重启。
>
> **核心观念**：bot 是用 `tmux` / `nohup` 启动的，SSH 断开**不会**杀掉它；  
> 但如果当初是在 vis 的终端里**直接**跑的，vis 一断 bot 就跟着一起没了。  
> 所以第一步永远是：先判断 bot 是「活着」还是「已经死了」。

---

## 一、先把服务器连回来（不依赖 vis）

打开你电脑上的「终端」（Mac 自带 Terminal，或 iTerm；访达里搜"终端"也行）：

```bash
ssh ubuntu@43.133.201.104
```

- 第一次会问 `Are you sure you want to continue connecting?` → 输入 `yes` 回车
- 然后输入你的登录密码
- 腾讯云如果给的是**密钥文件**而不是密码，用这行：
  ```bash
  ssh -i ~/.ssh/你的密钥.pem ubuntu@43.133.201.104
  ```

- 出现 `ubuntu@...:~$` 这种提示符，就说明进去了。

如果 vis 是 VS Code：左下角蓝色图标 → 选 `Remote-SSH: Connect to Host` → 选你的服务器名，或点 `Reconnect`。  
但**终端最稳**，建议以后都用终端 + tmux，别依赖 vis。

---

## 二、进去后，先判断 bot 是"活着"还是"已经死了"

```bash
tmux ls
```

- **有输出**（比如 `arb: 1 windows`）→ 当初用 tmux 跑的，bot 大概率还活着。  
  进去看实时画面：`tmux attach -t arb`  
  （看完了按 `Ctrl+b` 再按 `d` 退出，**不会杀进程**）
- **没输出 / 提示 `no server running`** → 没用 tmux。继续看进程：

```bash
ps aux | grep main.py | grep -v grep
```

- **有输出**（带 PID 那一行）→ bot 还活着，跳到第三步查"为什么没交易"
- **没输出** → bot **已经死了**（基本就是当初在 vis 终端里直接跑的，vis 一断 bot 就没了）。跳到第四步重启

> 也可以看 pid 文件确认：`cat ~/entropy-arb/logs/live.pid`  
> （目录名按你实际的来，可能是 `~/entropy-arb` 或 `~/entropy-rblighter`）

---

## 三、bot 还活着，但半小时没交易 → 按顺序查

先 `cd` 进你的 bot 目录（常见是 `~/entropy-arb`），然后复制下面整段：

```bash
cd ~/entropy-arb
echo "=== 1. 最近日志 ===" && tail -n 30 logs/live.log
echo "=== 2. 最后一笔成交 ===" && tail -n 3 logs/trades.csv
echo "=== 3. 日志最后更新时间 ===" && stat -c '%y' logs/live.log
```

按结果对号入座：

| 现象                                                        | 可能原因                                 | 怎么办                          |
| --------------------------------------------------------- | ------------------------------------ | ---------------------------- |
| 日志完全不动（连采集样本都不新增）                                         | 进程**卡死**                             | 杀掉重启（第四步）                    |
| 日志里反复出现 `RECORD` / `samples`，但没成交                         | 处在"只采集"时段（run.sh 的 21:00–22:00 北京时间） | 这个时段**本来就不成交**，等 22:00 后自动恢复 |
| 日志提示 `position limit` / `max_position`                    | 仓位到上限，不再加仓                           | 等价格回落平仓，或调大 `max_position`   |
| 日志有 `connection reset` / `timeout` / `401` / `rate limit` | 网络或 API 鉴权抖动                         | 一般会自动重试；卡太久就重启               |
| 没报错，但 `minutes.csv` 的 premium 一直没触达 `midline±阈值`          | 价差太薄（SNDK 本身就薄，之前实测约 1 笔/小时）         | 正常现象；想多成交就放松阈值，但单笔利润更薄       |

> 小知识：SNDK 流动性薄，阈值又紧的话，半小时没成交**可能是正常的，不是 bug**。  
> 用 `wc -l logs/trades.csv` 对比一小时前的行数，涨了就说明还在正常跑。

---

## 四、确认死了 / 卡了 → 重启（务必用 tmux 包一层）

```bash
pkill -f "main.py"            # 先清掉残留进程
tmux new -s arb               # 开一个 tmux 会话
```

进入 tmux 后，在里面启动 bot（二选一）：

```bash
# 方式 A：智能模式（推荐，含采集/交易自动切换、人类化抖动）
bash ~/entropy-arb/run.sh SNDK

# 方式 B：裸实盘
cd ~/entropy-arb && source .venv/bin/activate
nohup python3 main.py --symbol SNDK --hedge lighter-rh --cn >> logs/live.log 2>&1 &
```

启动后按 `Ctrl+b` 再按 `d` → **退出 tmux，但 bot 继续在后台跑**。  
以后 vis 再断、电脑关机都不影响它。

> 如果 run.sh 在某个目录，记得先 `cd` 过去再 `bash run.sh SNDK`。  
> 新服务器（rblighter 部署）目录是 `~/entropy-rblighter`，把上面路径换掉即可。

---

## 五、懒人一键体检（粘进去就能看现状）

```bash
cd ~/entropy-arb && \
echo "=== 进程 ===" && (ps aux | grep main.py | grep -v grep || echo "没在跑！") && \
echo "=== tmux ===" && (tmux ls 2>/dev/null || echo "无 tmux") && \
echo "=== 最近日志 ===" && tail -n 15 logs/live.log && \
echo "=== 最后一笔成交 ===" && tail -n 2 logs/trades.csv && \
echo "=== 成交总数 ===" && wc -l logs/trades.csv
```



---

## 防复发提醒

1. **永远在 tmux 里启动 bot**，别在 vis 终端里裸跑。这样断开连接 bot 也不会死。
2. 想更稳，可以让 run.sh 在服务器开机/崩了之后自动拉起（需要的话我可以帮你加 systemd 或开机脚本）。
3. 新服务器（rblighter）上线后，这套方法完全通用，只是目录换成 `~/entropy-rblighter`。
